<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WhatsApp Clone</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>💬 WhatsApp Clone</h1>
      <a href="/chats/new" class="btn-primary">+ New Chat</a>
    </header>

    <main>
      <% if (!chats || chats.length === 0) { %>
        <div class="empty-state">
          <div class="empty-icon">💬</div>
          <h2>No chats yet!</h2>
          <p>Start a conversation by creating a new chat</p>
          <a href="/chats/new" class="btn-secondary">Create First Chat</a>
        </div>
      <% } else { %>
        <div class="chats-list">
          <% for (let chat of chats) { %>
            <div class="chat-card">
              <div class="chat-header">
                <div class="chat-names">
                  <span class="from"><strong><%= chat.from || 'Unknown' %></strong></span>
                  <span class="arrow">→</span>
                  <span class="to"><strong><%= chat.to || 'Unknown' %></strong></span>
                </div>
                <div class="chat-actions">
                  <a href="/chats/<%= chat._id %>/edit" class="btn-edit">✏️ Edit</a>
                  <form method="POST" action="/chats/<%= chat._id %>?_method=DELETE" class="delete-form" onsubmit="return confirm('Are you sure?')">
                    <button type="submit" class="btn-delete">🗑️ Delete</button>
                  </form>
                </div>
              </div>
              
              <div class="chat-message">
                <p><%= chat.msg || '(no message)' %></p>
              </div>
              
              <div class="chat-footer">
                <span class="timestamp">
                  📅 Created: 
                  <% if (chat.createdAt) { %>
                    <%= chat.createdAt.toLocaleString() %>
                  <% } else if (chat.create) { %>
                    <%= chat.create.toLocaleString() %>
                  <% } else { %>
                    N/A
                  <% } %>
                </span>
                <span class="timestamp">
                  🔄 Updated: 
                  <% if (chat.updatedAt) { %>
                    <%= chat.updatedAt.toLocaleString() %>
                  <% } else if (chat.updatedate) { %>
                    <%= chat.updatedate.toLocaleString() %>
                  <% } else { %>
                    N/A
                  <% } %>
                </span>
              </div>
            </div>
          <% } %>
        </div>
      <% } %>
    </main>
  </div>
</body>
</html>