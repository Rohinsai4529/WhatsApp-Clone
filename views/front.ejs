<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WhatsApp Clone</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>💬 WhatsApp Clone</h1>
      <a href="/chats/new" class="btn-primary">+ New Chat</a>
    </header>

    <main>
      <% if (chats.length === 0) { %>
        <div class="empty-state">
          <div class="empty-icon">💬</div>
          <h2>No chats yet!</h2>
          <p>Start a conversation by creating a new chat</p>
          <a href="/chats/new" class="btn-secondary">Create First Chat</a>
        </div>
      <% } else { %>
        <div class="chats-list">
          <% for (let chat of chats) { %>
            <div class="chat-card">
              <div class="chat-header">
                <div class="chat-names">
                  <span class="from"><strong><%= chat.from %></strong></span>
                  <span class="arrow">→</span>
                  <span class="to"><strong><%= chat.to %></strong></span>
                </div>
                <div class="chat-actions">
                  <a href="/chats/<%= chat._id %>/edit" class="btn-edit">✏️ Edit</a>
                  <form method="POST" action="/chats/<%= chat._id %>?_method=DELETE" class="delete-form" onsubmit="return confirm('Are you sure you want to delete this chat?')">
                    <button type="submit" class="btn-delete">🗑️ Delete</button>
                  </form>
                </div>
              </div>
              
              <div class="chat-message">
                <p><%= chat.msg %></p>
              </div>
              
              <div class="chat-footer">
                <span class="timestamp">
                  📅 Created: <%= chat.createdAt.toLocaleString('en-US', { 
                    month: 'short', 
                    day: 'numeric', 
                    year: 'numeric', 
                    hour: '2-digit', 
                    minute: '2-digit' 
                  }) %>
                </span>
                <span class="timestamp">
                  🔄 Updated: <%= chat.updatedAt.toLocaleString('en-US', { 
                    month: 'short', 
                    day: 'numeric', 
                    year: 'numeric', 
                    hour: '2-digit', 
                    minute: '2-digit' 
                  }) %>
                </span>
              </div>
            </div>
          <% } %>
        </div>
      <% } %>
    </main>


  </div>
</body>
</html>